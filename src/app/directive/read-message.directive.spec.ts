import { ReadMessageDirective } from './read-message.directive';

describe('ReadMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new ReadMessageDirective();
    expect(directive).toBeTruthy();
  });
});
